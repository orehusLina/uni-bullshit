#include <iostream>
#include <vector>
using namespace std;
int main() {
    vector<int> sverhy;
    int counterRes = 0;
    int sleva = 0;
    int n, m;
    cin >> n >> m;
    int res[n][m];
    for (int q = 0; q < n; ++q) {
        string s; cin >> s;
        for (int i = 0; i < m; ++i) {
            if (s[i] == '.') {
                int sleva = 0;
            }
            if (s[i] == '*') {
                
            }
        }
    }
}